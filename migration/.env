# Database Migration Configuration - Test Setup

# MongoDB Atlas Connection (Source Database) 
# For demonstration purposes, using a test/demo connection
MONGODB_URI=mongodb://localhost:27017
DB_NAME=jobLander

# Migration Settings
BATCH_SIZE=5
DRY_RUN=true

# Note: AWS Amplify Configuration is automatically loaded from amplify_outputs.json
# The migration script will use the current deployed Amplify infrastructure